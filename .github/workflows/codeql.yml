name: CodeQL

on:
  workflow_dispatch:
jobs:
  analyze:
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: agent-ix/nodejs-actions/.github/workflows/security-scan.yml@main
