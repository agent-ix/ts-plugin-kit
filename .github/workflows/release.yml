name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: agent-ix/nodejs-actions/pull@main
      - uses: agent-ix/nodejs-actions/ghcr-docker-summary@main
