name: CodeQL

on:
  schedule:
    - cron: "0 6 * * 1"

  workflow_dispatch:
jobs:
  analyze:
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: agent-ix/nodejs-actions/.github/workflows/security-scan.yml@main
